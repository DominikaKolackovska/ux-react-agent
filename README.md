# UX ReAct Agent

LLM-based UX analysis agent using ReAct pattern and tool calling.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env